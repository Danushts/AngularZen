import { ZenIfDirective } from './zen-if.directive';

describe('ZenIfDirective', () => {
  it('should create an instance', () => {
    const directive = new ZenIfDirective();
    expect(directive).toBeTruthy();
  });
});
