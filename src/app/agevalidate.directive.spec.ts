import { AgevalidateDirective } from './agevalidate.directive';

describe('AgevalidateDirective', () => {
  it('should create an instance', () => {
    const directive = new AgevalidateDirective();
    expect(directive).toBeTruthy();
  });
});
