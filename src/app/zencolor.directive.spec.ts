import { ZencolorDirective } from './zencolor.directive';

describe('ZencolorDirective', () => {
  it('should create an instance', () => {
    const directive = new ZencolorDirective();
    expect(directive).toBeTruthy();
  });
});
